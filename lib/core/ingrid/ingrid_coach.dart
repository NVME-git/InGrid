import '../engine/board.dart';

/// Ingrid coaching engine — stub, logic not yet implemented.
class IngridCoach {
  /// Returns a hint for the current board state.
  /// Returns null when no hint is available (stub always returns null).
  String? getHint(SudokuBoard board) => null;
}
